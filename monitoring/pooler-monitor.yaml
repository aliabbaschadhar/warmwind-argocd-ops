apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: postgres-ha-pooler-monitor
  namespace: database
  labels:
    release: prometheus-stack
spec:
  selector:
    matchLabels:
      cnpg.io/podRole: pooler
  podMetricsEndpoints:
  - port: metrics
    interval: 15s
