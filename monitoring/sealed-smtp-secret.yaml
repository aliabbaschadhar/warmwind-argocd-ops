apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: alertmanager-smtp-secret
  namespace: database
spec:
  # The encryptedData fields below are placeholders.
  # Generate the real encrypted values using the kubeseal CLI.
  encryptedData:
    password: rpdi nner fqjt occg
  template:
    metadata:
      name: alertmanager-smtp-secret
      namespace: database
    type: Opaque
