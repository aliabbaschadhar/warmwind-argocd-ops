apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: haproxy-monitor-resources
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'git://git-server.gitops.svc.cluster.local/repo.git'
    targetRevision: HEAD
    path: haproxy
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: ingress-controller
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
